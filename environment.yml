name: flem
channels:
  - conda-forge
  - defaults
  - pyviz
dependencies:
  # these are for repo2docker so that mybinder works

  # these are the ones we need for the code and notebooks to work
  - fenics=2019.1.0=py37_1
  - mshr=2019.1.0=py37h7596e34_1000
  - gdal
  - peakutils
  - matplotlib
  - scipy
  - voila
  - vtk
  - ipywidgets
  - itkwidgets
  - bokeh
  - pyviz_comms
  - panel
  - nodejs
  - pandas
  - geopandas
  - pip
  - pip:
      - elevation
      - pyvista
      - PyQt5

prefix: /srv/conda
